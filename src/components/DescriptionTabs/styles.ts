import { Tabs } from "react-tabs";
import styled from "styled-components";
import { Margin1 } from "ui";

export const StyledTabs = styled(Tabs)`
  min-height: 200px;
  margin-bottom: ${Margin1.desktop};
`;
