export enum ROUTE {
  HOME = "react-bookstore/",
  SEARCH = "search/:searchValue/:page",
  FAVORITES = "favorites",
  PROFILE = "profile",
  SIGNUP = "auth",
  SHOP_BAG = "order",
  DETAILS = "/books/:isbn13",
  RESETPASS = "/reset",
}
