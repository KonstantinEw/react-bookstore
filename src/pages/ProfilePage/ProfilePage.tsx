import { ArrowBackButton, Profile, Title } from "components";

export const ProfilePage = () => {
  return (
    <section>
      <ArrowBackButton />
      <Title>account</Title>
      <Profile />
    </section>
  );
};
