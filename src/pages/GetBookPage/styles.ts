import { motion } from "framer-motion";
import styled from "styled-components";

const StyledBookPage = styled(motion.section)`
  display: flex;
  flex-direction: column;
`;

export { StyledBookPage };
